spring:
  application:
    name: bank-reconciliation-api
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_reconciliation
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true